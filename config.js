module.exports = {
    // Deploy task
    deploy: {
        //empty for now
    },
    firebase: {
        env: {
            default: {
                url: "https://dragodindefactory.firebaseio.com/",
                config: {}
            }
        },
        global: {
            config: {
                //empty too
            }
        }
    }
}
